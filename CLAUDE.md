# CLAUDE.md

Este arquivo existe apenas por convenção (Claude Code carrega `CLAUDE.md` automaticamente ao abrir o projeto).

**Toda a informação operacional do projeto está em [`AGENTS.md`](./AGENTS.md).**

Antes de executar qualquer tarefa neste repositório — corrigir bug, implementar feature, revisar código, responder dúvida sobre a lógica —, leia `AGENTS.md` na raiz do projeto. Ele é a fonte única de verdade sobre:

- o que o projeto é e para quem;
- como o código está (e deve ficar) estruturado;
- quais regras de negócio são invioláveis;
- o processo a seguir para qualquer demanda (spec-driven);
- o que nunca fazer neste código.

Não duplique, resuma ou reinterprete o conteúdo de `AGENTS.md` aqui. Se `AGENTS.md` for atualizado, este arquivo não precisa mudar — ele só aponta para lá.
